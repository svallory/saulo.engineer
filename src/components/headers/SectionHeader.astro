---
interface Props {
  title: string
  subtitle?: string
}

const { title, subtitle } = Astro.props
---

<div class="mt-30 mb-20 text-center">
  <h2 class="text-6xl">{title}</h2>
  {subtitle && <p class="text-2xl text-red-400">{subtitle}</p>}
</div>
